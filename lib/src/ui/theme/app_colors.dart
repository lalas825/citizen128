import 'dart:ui';

class AppColors {
  static const Color navyBlue = Color(0xFF1A237E); 
  static const Color federalBlue = Color(0xFF112D50); // Keep existing dark blue for text? Or replace?
  // "Navy Blue" (0xFF1A237E) requested for buttons/headers.
  // Existing "Brand Teal" (0xFF00C4B4) is used for accents.
  
  static const Color teal = Color(0xFF00C4B4);
}
